package skymaps

version v2r7p2

# $Header$author  T. Burnett <tburnett@u.washington.edu>
author  T. Burnett <tburnett@u.washington.edu>


use astro *
use healpix *
use st_facilities *
use ROOT v* IExternal

# SWIG interface
use SwigModule *
apply_pattern python_module

# instead of python_module
#apply_pattern shared_library
private

use embed_python * 

macro source "*.cxx" WIN32 "*.cxx *.h *.py"

# for numpy
# Note that it is "Lib" in windows, but "lib" on linux. So lib should work for both
macro_append use_includes " $(ppcmd) $(python_PATH)/lib/site-packages/numpy/core/include" 

library skymaps \
 -s=../skymaps $(source) \
 -s=../src $(source) \
 -s=../python $(source)

application test_skymaps -s=test $(source)

macro swig_include "-I${skymaps_root}\
 -I${astro_root}\
 -I${healpix_root}\
 -I${CLHEPBASE}/include"


